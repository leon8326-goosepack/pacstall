# Install pacman, without a package manager.
This requires git, make and gcc.

## Installation
### CURL

```
sudo su -
bash <(curl -s https://raw.githubusercontent.com/leon8326-goosepack/pacstall/main/run.sh)
```
